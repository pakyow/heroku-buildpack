#!/usr/bin/env ruby

puts <<~YAML
  ---
  addons:
    - heroku-postgresql
    - heroku-redis
  default_process_types:
    release: pakyow prelaunch:release -e production
    web: pakyow boot -e production
YAML
