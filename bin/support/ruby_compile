#!/usr/bin/env ruby

# Compiles a Pakyow project to run on Heroku.

$stdout.sync = true

$:.unshift File.expand_path("../../../lib", __FILE__)

require "pakyow/buildpack"

Pakyow::Buildpack::Base.new(
  Pakyow::Buildpack::Config.new(*ARGV)
).compile
